*** Begin Patch
*** Update File: frameworks/base/packages/SettingsLib/Spa/spa/src/com/android/settingslib/spa/framework/theme/SettingsRadius.kt
@@
 import androidx.compose.ui.unit.dp
 
 object SettingsRadius {
+    private val omarchy = android.os.Build.BRAND.equals("Omarchy", ignoreCase = true)
     val none = 0.dp
     val extraSmall2 = 4.dp
-    val medium = 12.dp
-    val large1 = 16.dp
-    val large2 = 20.dp
-    val large3 = 24.dp
-    val extraLarge1 = 28.dp
+    val medium = if (omarchy) 4.dp else 12.dp
+    val large1 = if (omarchy) 4.dp else 16.dp
+    val large2 = if (omarchy) 4.dp else 20.dp
+    val large3 = if (omarchy) 4.dp else 24.dp
+    val extraLarge1 = if (omarchy) 4.dp else 28.dp
 }
*** End Patch
